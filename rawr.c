// drameloN @23:54 4Jul2024
// Template only

#include <stdio.h>
#include <math.h>
#include <string.h>

int main() {
    int a;
    scanf(" %d", &a);

    int b;
    scanf(" %d", &b);
    int c[b];
    for (int i=0; i<b; i++){scanf(" %d", &c[i]); };

    return 0;
}